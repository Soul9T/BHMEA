<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Black Hat MEA — Team Quick Access</title>

  <!-- Google font -->
  <link rel="preconnect" href="https://fonts.googleapis.com"><link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">

  <style>
    :root{
      /* [EDIT] Optional: tweak colors to match the event brand */
      --black:#050608; --bg:#050608; --bg2:#050910; --card:#0b0f19; --muted:#9aa4b2; --text:#f5f7fa;
      --accent1:#02f39a; --accent2:#00c8ff; --border:#222739; --danger:#ff5257;
      --ring:0 0 0 2px rgba(0,200,255,.35),0 0 24px rgba(2,243,154,.25);
    }
    *{box-sizing:border-box}
    body{
      margin:0; font-family:Inter,system-ui,Segoe UI,Roboto,Helvetica,Arial,sans-serif;
      color:var(--text,#f5f7fa); background-color:var(--bg,#050608);
      background:
        radial-gradient(1200px 640px at 80% -10%, rgba(0,200,255,.16), transparent 55%),
        radial-gradient(1000px 700px at -10% 110%, rgba(2,243,154,.12), transparent 60%),
        linear-gradient(180deg, var(--bg,#050608), var(--bg2,#050910));
      min-height:100vh;
    }
    .wrap{max-width:1120px;margin:34px auto;padding:0 20px}
    header{display:flex;gap:16px;justify-content:space-between;align-items:flex-start;flex-wrap:wrap}
    h1{margin:0;font-size:clamp(22px,3.4vw,32px)}
    .accent{background:linear-gradient(90deg,var(--accent1),var(--accent2));-webkit-background-clip:text;background-clip:text;color:transparent}
    .note{color:var(--muted);font-size:13px;margin-top:6px}

    nav{display:flex;gap:8px;flex-wrap:wrap;align-items:center}
    nav a{color:var(--muted);text-decoration:none;padding:10px 14px;border-radius:999px;border:1px solid var(--border);background:#050910;transition:all .2s}
    nav a:hover{box-shadow:var(--ring);color:#fff}
    nav a.active{color:#050608;border:1px solid transparent;background:linear-gradient(#02f39a,#02f39a) padding-box,linear-gradient(135deg,#02f39a,#00c8ff) border-box}

    .toolbar{display:flex;gap:8px;margin:14px 0 6px;flex-wrap:wrap}
    .btn{border:1px solid var(--border);background:#050910;color:#f2f5f9;padding:8px 12px;border-radius:10px;cursor:pointer;transition:all .2s}
    .btn:hover{box-shadow:var(--ring)}

    .page{margin-top:10px}.hide{display:none}

    .accordion{display:grid;gap:14px}
    details{border:1px solid transparent;border-radius:16px;overflow:hidden;position:relative;background:linear-gradient(var(--card),var(--card)) padding-box,linear-gradient(135deg, rgba(0,200,255,.85), rgba(2,243,154,.9)) border-box;transition:transform .12s}
    details:hover{transform:translateY(-1px)}
    details.danger{background:linear-gradient(var(--card),var(--card)) padding-box,linear-gradient(135deg, rgba(255,82,87,.95), rgba(255,123,102,.9)) border-box}
    summary{list-style:none;padding:18px 22px;cursor:pointer;display:flex;align-items:center;gap:12px;font-weight:800;user-select:none}
    summary::-webkit-details-marker{display:none}
    .chev{flex:0 0 22px;height:22px;border-radius:6px;border:1px solid var(--border);display:grid;place-items:center;transition:transform .18s;background:linear-gradient(180deg,#101624,#050910)}
    .chev svg{width:14px;height:14px} details[open] .chev{transform:rotate(90deg)}
    .panel{padding:0 22px 22px}

    ul.list{margin:10px 0 0;padding:0;list-style:none;display:grid;gap:10px}
    .row{display:flex;align-items:flex-start;gap:10px;border:1px dashed var(--border);border-radius:12px;padding:10px 12px;background:#070c15}
    .label{font-weight:800;letter-spacing:.2px}
    .vals{display:flex;flex-wrap:wrap;gap:6px}
    .pill{background:#050910;border:1px solid var(--border);border-radius:999px;padding:3px 9px;font-size:13px}
    .mono{font-family:ui-monospace,SFMono-Regular,Menlo,Monaco,Consolas,"Liberation Mono","Courier New",monospace}
    .chip{margin-left:auto;font-size:12px;border:1px solid var(--border);padding:3px 8px;border-radius:999px;cursor:pointer;background:#050910;color:#dfe6ee}
    .chip.good{border-color:#02f39a;color:#050608;background:#02f39a}

    .hero{display:grid;gap:12px;margin:10px 0 12px}
    .search-big{width:100%;padding:16px 18px;border-radius:14px;border:1px solid var(--border);background:#050910;color:#f2f5f9;font-size:18px}
    .search-big:focus{outline:none;box-shadow:var(--ring)}
    .plan{width:100%;max-width:100%;border-radius:12px;border:1px solid var(--border)}
    table{width:100%;border-collapse:collapse;margin-top:8px}
    th,td{padding:12px;border-bottom:1px solid var(--border);text-align:left}
    th{color:var(--muted);font-weight:600}
    tr.off{display:none}

    .lightbox{position:fixed;inset:0;background:rgba(0,0,0,.85);display:none;align-items:center;justify-content:center;z-index:9999}
    .lightbox.show{display:flex}
    .lightbox img{max-width:95vw;max-height:90vh;border-radius:12px;border:1px solid #2a2f40;box-shadow:0 20px 60px rgba(0,0,0,.6)}

    ol.top10{margin:8px 0 0 22px}
    ol.top10 li{padding:10px 8px;border-bottom:1px dashed var(--border)}
    ol.top10 li:last-child{border-bottom:none}
    .stats-wrap{margin-top:12px}
    .stats-grid{display:grid;grid-template-columns:repeat(12,minmax(0,1fr));grid-auto-rows:110px;gap:12px}
    .stat{background:#070c15;border:1px solid var(--border);border-radius:14px;display:flex;flex-direction:column;justify-content:center;padding:16px 18px}
    .knum{font-size:28px;font-weight:800;line-height:1}
    .lbl{font-size:12px;color:var(--muted);margin-top:6px}
    .span-2{grid-column:span 2}.span-3{grid-column:span 3}.span-4{grid-column:span 4}.span-5{grid-column:span 5}
    @media(max-width:740px){.stats-grid{grid-template-columns:repeat(6,minmax(0,1fr));grid-auto-rows:100px}
      .span-5{grid-column:span 3}.span-4{grid-column:span 3}.span-3{grid-column:span 3}.span-2{grid-column:span 3}}

    @media (max-width: 540px) {
      html { font-size: 13.5px; }
      h1 { font-size: 20px; }
      .note { font-size: 11.5px; }
      nav a { padding: 6px 10px; font-size: 12.5px; }
      summary { padding: 12px 14px; font-size: 14px; }
      .panel  { padding: 0 14px 12px; }
      .list { gap: 6px; }
      .row  { padding: 7px 9px; gap: 6px; }
      .label{ font-weight: 700; }
      .pill  { font-size: 10.5px; padding: 2px 7px; }
      .search-big { font-size: 15px; padding: 12px 14px; }
      table th, table td { font-size: 12.5px; padding: 9px; }
    }
    @media (max-width: 380px) {
      html { font-size: 12.5px; }
      .pill { font-size: 10px; }
    }
  </style>
</head>
<body>
<div class="wrap">
  <header>
    <div>
      <!-- [EDIT] Change the event name here -->
      <h1><span class="accent">Black Hat MEA 2025</span> — Team Quick Access</h1>
      <div class="note">Optimized for mobile. Tap to expand.</div>
      <!-- Optional short usage note for the team -->
      <!-- Example: "Edit only the CONFIG section at the bottom of this file (POCS, AREAS_DATA, TOP10)." -->
    </div>
    <nav id="nav">
      <a data-page="pocs" class="active" href="#pocs">POCs & Info</a>
      <a data-page="areas" href="#areas">Area Map</a>
      <a data-page="top10s" href="#top10s">Top 10s</a>
    </nav>
  </header>

  <div class="toolbar" id="toolbar">
    <button class="btn" id="expandAll">Expand all</button>
    <button class="btn" id="collapseAll">Collapse all</button>
  </div>

  <section id="pocs" class="page">
    <div class="accordion" id="pocsAcc"></div>
  </section>

  <section id="areas" class="page hide">
    <div class="hero">
      <input id="searchAreas" class="search-big" type="search" placeholder="Search areas, halls, locations…" />
      <!-- [EDIT] Replace Floorplan.png with your own map image but keep the same file name, or change the src below -->
      <img id="planThumb" class="plan" src="Floorplan.png" alt="Event Floorplan overview" />
      <span class="note">Tap map to zoom. Results below are A→Z.</span>
    </div>
    <table id="areasTable">
      <thead><tr><th>AREA / FEATURE</th><th>Hall</th><th>LOCATION</th></tr></thead>
      <tbody></tbody>
    </table>
  </section>

  <section id="top10s" class="page hide">
    <section class="accordion" id="topAcc"></section>
  </section>
</div>

<div id="lightbox" class="lightbox" aria-hidden="true">
  <img id="lightboxImg" src="Floorplan.png" alt="Floorplan enlarged" />
</div>

<!-- ================== CONFIG TO EDIT (CONTENT ONLY) ================= -->
<script>
/*
  HOW TO USE (Share this with the team):

  1) You ONLY need to edit the blocks below: POCS, AREAS_DATA, TOP10.
  2) Do not touch anything after the line that says "RENDERING" unless you know HTML/JS.
  3) Inside each block, change the text but keep the structure:
       - Each line in POCS becomes one row.
       - Use "Label : Value" for Wi-Fi and timing items.
       - Use "Responsibility — Name" for people assignments.
       - For AREAS_DATA, each item is { feature, hall, location }.
       - In TOP10, each name on its own line.
  4) Save the file as index.html, commit/push to GitHub, and enable GitHub Pages.
*/

/* POCs & Info */
const POCS = {
  // [EDIT] Wi-Fi / codes / generic quick info
  wifi: `
Staff WiFi : [NETWORK NAME]
Password : [PASSWORD]
Speaker WiFi : [NETWORK NAME]
Speaker Password : [PASSWORD]
Door code (back of house) : [IF APPLICABLE]
How to enter the code : [SHORT INSTRUCTIONS]
`,

  // [EDIT] Critical / emergency info
  critical: `
Emergency (Medical) — [LOCATION / HALL]
Security Control Room — [LOCATION / PHONE]
Lost & Found — [LOCATION]
Venue Duty Manager — [NAME / PHONE]
`,

  // [EDIT] Show timings (registration, expo, content, etc.)
  timings: `
Registration Opens — [HH:MM] → Closes — [HH:MM]
Show Open — [HH:MM] → Show Close — [HH:MM]
Content Starts — [HH:MM] → Ends — [HH:MM]
`,

  // [EDIT] Stages / tracks and their POCs
  stages: `
Main Stage — [NAME]
Stage 2 — [NAME]
Stage 3 — [NAME]
Workshops — [NAME]
Labs / Hacking Arena — [NAME]
Sponsor Demo Stage — [NAME]
`,

  // [EDIT] Ops responsibilities and owners
  ops: `
Registration — [NAME]
Badging / Access Control — [NAME]
Catering — [NAME]
Cleaning — [NAME]
Furniture — [NAME]
Internet / Wi‑Fi — [NAME]
Venue Management — [NAME]
Security — [NAME]
Traffic / Parking — [NAME]
Wayfinding / Branding — [NAME]
`,

  // [EDIT] Marketing / content responsibilities
  marketing: `
Design — [NAME]
Social Media (Onsite) — [NAME]
Content Capture / Video — [NAME]
PR — [NAME]
Reg Stats / Dashboards — [NAME]
VIP Comms — [NAME]
`,

  // [EDIT] Protocol / VIP / government
  protocol: `
Government & Protocol Support — [NAME]
VIP Tours — [NAME]
VIP Lounge Operations — [NAME]
Government Catering & Crowd Support — [NAME]
Key Escalation (urgent) — [NAME]
`,

  // [EDIT] Key locations to find quickly
  locations: `
Info Desk — [LOCATION]
Medical — [LOCATION]
VIP Lounge — [LOCATION]
Speaker Lounge — [LOCATION]
Merch Store — [LOCATION]
Prayer Rooms — [LOCATION]
`,

  // [EDIT] App / tech support
  App: `
Event App Support — [NAME / EMAIL]
Badge / QR Support — [NAME / EMAIL]
`
};

/* Areas data (area map table)
   Each object = one row in the table. Duplicate and edit as needed. */
const AREAS_DATA = [
  // [EXAMPLE ROWS] — change to real Black Hat (or event) areas
  { feature:"Registration",          hall:"[HALL]",  location:"[Exact location text]" },
  { feature:"Main Stage",            hall:"[HALL]",  location:"[Exact location text]" },
  { feature:"Sponsor Expo",          hall:"[HALL]",  location:"[Exact location text]" },
  { feature:"Labs / Hacking Arena",  hall:"[HALL]",  location:"[Exact location text]" },
  { feature:"Speaker Lounge",        hall:"[HALL]",  location:"[Exact location text]" },
  { feature:"VIP Lounge",            hall:"[HALL]",  location:"[Exact location text]" },
  { feature:"Food Court",            hall:"[HALL]",  location:"[Exact location text]" },
  { feature:"Prayer Rooms",          hall:"[HALL]",  location:"[Exact location text]" },
];

/* Top 10s / key stats */
const TOP10 = {
  // [EDIT] Top investors / partners / strategic guests (or delete this block)
  investors: `
[Name 1]
[Name 2]
[Name 3]
[Name 4]
[Name 5]
[Name 6]
[Name 7]
[Name 8]
[Name 9]
[Name 10]
`,

  // [EDIT] Top speakers you want the team to remember
  speakers: `
[Speaker 1]
[Speaker 2]
[Speaker 3]
[Speaker 4]
[Speaker 5]
[Speaker 6]
[Speaker 7]
[Speaker 8]
[Speaker 9]
[Speaker 10]
`,

  // [EDIT] Top sponsors or exhibitors
  exhibitors: `
[Sponsor 1]
[Sponsor 2]
[Sponsor 3]
[Sponsor 4]
[Sponsor 5]
[Sponsor 6]
[Sponsor 7]
[Sponsor 8]
[Sponsor 9]
[Sponsor 10]
`,

  // [EDIT] Final stats (format: Label: [value])
  finalStats: `
Attendees: [XXXX]
Countries: [XX]
Speakers: [XXX]
Sponsors & Exhibitors: [XXX]
`
};
</script>

<!-- ================== RENDERING (DO NOT EDIT BELOW THIS LINE) ================= -->
<script>
const $  = s => document.querySelector(s);
const $$ = s => Array.from(document.querySelectorAll(s));
const li = s => s.trim().split(/\r?\n/).map(x=>x.trim()).filter(Boolean);
const pairs = s => li(s).map(line=>{
  let i=line.indexOf(':'); if(i>-1) return {k:line.slice(0,i).trim(), v:line.slice(i+1).trim(), kind:'kv'};
  i=line.indexOf(' — '); if(i>-1) return {k:line.slice(0,i).trim(), v:line.slice(i+3).trim(), kind:'role'};
  i=line.indexOf(' - '); if(i>-1) return {k:line.slice(0,i).trim(), v:line.slice(i+3).trim(), kind:'role'};
  return {k:'', v:line, kind:'raw'};
});

function prettyList(block, {copyPasswords=false}={}){
  const items = pairs(block).map(obj=>{
    let label = obj.k ? `<span class="label">${obj.k}</span>` : '';
    let vals  = '';
    if(obj.kind === 'kv'){
      const isPass = /password/i.test(obj.k);
      vals = `<span class="vals"><span class="mono">${obj.v}</span></span>` +
             (isPass && copyPasswords ? ` <span class="chip" data-copy="${obj.v}">Copy password</span>` : '');
    } else if(obj.kind === 'role'){
      const parts = obj.v.split(/\/|,|;| & | and /i).map(s=>s.trim()).filter(Boolean);
      vals = `<span class="vals">` + (parts.length?parts:[obj.v]).map(p=>`<span class="pill">${p}</span>`).join('') + `</span>`;
    } else {
      vals = `<span>${obj.v}</span>`;
    }
    return `<li class="row">${label}${vals}</li>`;
  }).join('');
  return `<ul class="list">${items}</ul>`;
}

(function(){
  const sections = [
    {title:'Wi‑Fi & Codes',          html: prettyList(POCS.wifi,     {copyPasswords:true})},
    {title:'Critical Info',          html: prettyList(POCS.critical), danger:true},
    {title:'Show Timings',           html: prettyList(POCS.timings)},
    {title:'Stage Info & POCs',      html: prettyList(POCS.stages)},
    {title:'Ops POCs',               html: prettyList(POCS.ops)},
    {title:'App Support',            html: prettyList(POCS.App)},
    {title:'Marketing & Comms',      html: prettyList(POCS.marketing)},
    {title:'Protocol / VIP',         html: prettyList(POCS.protocol)},
    {title:'Key Locations',          html: prettyList(POCS.locations)}
  ];
  const acc = $('#pocsAcc');
  sections.forEach(sec=>{
    const d=document.createElement('details'); if(sec.danger) d.classList.add('danger');
    d.innerHTML = `
      <summary><span class="chev"><svg viewBox="0 0 24 24" fill="none"><path d="M8 5l8 7-8 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg></span>${sec.title}</summary>
      <div class="panel">${sec.html}</div>`;
    acc.appendChild(d);
  });
  acc.addEventListener('click', e=>{
    const btn = e.target.closest('.chip'); if(!btn) return;
    const val = btn.getAttribute('data-copy'); if(!val) return;
    navigator.clipboard.writeText(val).then(()=>{
      const t=btn.textContent; btn.textContent='Copied!'; btn.classList.add('good');
      setTimeout(()=>{btn.textContent=t; btn.classList.remove('good')}, 1200);
    });
  });
})();

(function(){
  const skipRe=/toilet|restroom/i;
  const items = AREAS_DATA
    .filter(r=>!skipRe.test(r.feature))
    .sort((a,b)=>a.feature.localeCompare(b.feature,'en',{sensitivity:'base'}));

  const tbody = $('#areasTable tbody');
  items.forEach(r=>{
    const tr=document.createElement('tr');
    tr.innerHTML = `<td>${r.feature}</td><td>${r.hall}</td><td>${r.location}</td>`;
    tbody.appendChild(tr);
  });

  const rows = $$('#areasTable tbody tr');
  $('#searchAreas').addEventListener('input', e=>{
    const term = e.target.value.trim().toLowerCase();
    rows.forEach(tr=>tr.classList.toggle('off', !tr.textContent.toLowerCase().includes(term)));
  });

  const lb=$('#lightbox');
  $('#planThumb').addEventListener('click', ()=>{lb.classList.add('show');lb.setAttribute('aria-hidden','false')});
  lb.addEventListener('click', ()=>{lb.classList.remove('show');lb.setAttribute('aria-hidden','true')});
  document.addEventListener('keydown', e=>{if(e.key==='Escape') lb.classList.remove('show')});
})();

(function(){
  const acc=$('#topAcc');
  [
    {title:'Top 10 VIPs / Investors', data: li(TOP10.investors)},
    {title:'Top 10 Speakers',        data: li(TOP10.speakers)},
    {title:'Top 10 Sponsors',        data: li(TOP10.exhibitors)},
  ].forEach(sec=>{
    const d=document.createElement('details');
    d.innerHTML=`<summary><span class="chev"><svg viewBox="0 0 24 24" fill="none"><path d="M8 5l8 7-8 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg></span>${sec.title}</summary><div class="panel"><ol class="top10">${sec.data.map(x=>`<li>${x}</li>`).join('')}</ol></div>`;
    acc.appendChild(d);
  });

  const pairBlock = s => s.trim().split(/\r?\n/).map(x=>x.trim()).filter(Boolean).map(line=>{const i=line.indexOf(':');return i<0?{label:line,value:''}:{label:line.slice(0,i).trim(), value:line.slice(i+1).trim()}});
  const pairs = pairBlock(TOP10.finalStats);
  const widths=[4,3,2,3,4,3,2,3,4,3];
  const grid=document.createElement('div'); grid.className='stats-wrap';
  let inner=`<details><summary><span class="chev"><svg viewBox="0 0 24 24" fill="none"><path d="M8 5l8 7-8 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg></span>Final Key Stats</summary><div class="panel"><div class="stats-grid">`;
  pairs.forEach((p,i)=>{ const span=widths[i%widths.length]; inner+=`<div class="stat span-${span}"><div class="knum">${p.value||'-'}</div><div class="lbl">${p.label}</div></div>`; });
  inner+=`</div></div></details>`;
  grid.innerHTML=inner; acc.appendChild(grid);
})();

(function(){
  const pages=$$('.page'), links=$$('#nav a'), toolbar=$('#toolbar');
  function show(id){
    pages.forEach(p=>p.classList.toggle('hide', p.id!==id));
    links.forEach(a=>a.classList.toggle('active', a.dataset.page===id));
    toolbar.style.display = (id==='areas') ? 'none' : 'flex';
  }
  function init(){ const h=(location.hash||'#pocs').replace('#',''); show(['pocs','areas','top10s'].includes(h)?h:'pocs'); }
  window.addEventListener('hashchange', init); init();

  const getActive=()=> (links.find(a=>a.classList.contains('active'))||links[0]).dataset.page;
  $('#expandAll').addEventListener('click',()=>{ $(`#${getActive()}`).querySelectorAll('details').forEach(d=>d.open=true); });
  $('#collapseAll').addEventListener('click',()=>{ $(`#${getActive()}`).querySelectorAll('details').forEach(d=>d.open=false); });
})();
</script>
</body>
</html>
